<h2>Lista de Categorias</h2>
<hr>
<a href="./categorias/add" ><button class="btn btn-success mb-3">Nova Categorias </button></a>
{{#each categorias}}
    <div class="card mb-2">
        <div class="card-body mb-8">
            <h5 class="card-title">{{nome}}</h5>
            <h6 class="card-subtitle mb-2 text-muted">Slug: {{slug}}</h6> 
            <h6 class="card-subtitle mb-2 text-muted">data de criação: {{date}}</h6>
            <div class="row row-cols-2">
                <div class="col-md-auto">
                    <a href="/admin/categorias/edit/{{_id}}" ><button class="btn btn-primary btn-sm">Editar</button></a>
                </div>  
                <div class="col-md-auto">
                    <form action="/admin/categorias/deletar" method="POST">
                        <input type="hidden" name="id" value="{{_id}}">
                        <button type="submit" class="btn btn-danger btn-sm">Deletar</button>
                    </form>
                </div>      
            </div>
        </div>
        
    </div>
    {{else}}
    <h4 class="mt-3"> Nenuma categoria registrada</h4>
    <img src="/img/sem_resultados.jpg" class="img-thumbnail" width="400px" height="300px" alt="Sem resultados">
{{/each}}