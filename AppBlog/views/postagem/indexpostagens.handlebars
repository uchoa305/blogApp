<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <small>data de publicação: {{postagem.data}}</small>
        <small class="card-subtitle mb-3 text-muted">
        <hr>
        <h6 class="mt-3">{{postagem.descricao}}</h6>
        <p class="mt-3">{{postagem.conteudo}}</p>
    </div>    
</div>
